# Database Configuration for Docker
# PostgreSQL is running in Docker on port 5433 (mapped from internal 5432)

DATABASE_URL=postgresql://hotel_app:SecurePassword2024@localhost:5433/hotel_mapper

# Alternative connection parameters
DB_HOST=localhost
DB_PORT=5433
DB_NAME=hotel_mapper
DB_USER=hotel_app
DB_PASSWORD=SecurePassword2024

# Connection pool settings
DB_POOL_MIN=2
DB_POOL_MAX=10

# Docker container name
CONTAINER_NAME=hotel_mapper_db